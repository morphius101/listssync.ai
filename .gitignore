node_modules
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Replit internal state / agent binaries
.local/
.config/
replit.md
.env

# Local design/scratch files
/scratch/

# One-shot ops diagnostics — each phase produces its own; not production code.
# Use `git add -f diag-keeper.mjs` if a particular diag becomes worth keeping.
diag-*.mjs
